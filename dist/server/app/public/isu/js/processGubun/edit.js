'use strict';

$(document).ready(function() { 
    $('#use_yn').val(processGubunObj.use_yn);
    $('#higher_cd').val(processGubunObj.higher_cd);
    $('#question_type').val(processGubunObj.question_type);
});
